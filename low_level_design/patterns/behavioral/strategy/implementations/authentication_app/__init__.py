"""Problem: Apps support multiple login methods (Email/Password, OAuth, Biometric).

Strategy:

AuthStrategy with authenticate(user)

Strategies: PasswordAuth, OAuthAuth, BiometricAuth.

Example: Google Workspace, Microsoft Azure AD login."""